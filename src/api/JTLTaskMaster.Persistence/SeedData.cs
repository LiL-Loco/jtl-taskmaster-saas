using JTLTaskMaster.Persistence;
using JTLTaskMaster.Domain.Entities; using Microsoft.EntityFrameworkCore;  namespace JTLTaskMaster.Persistence;  public static class SeedData {     public static async Task Initialize(ApplicationDbContext context)     {         await context.Database.MigrateAsync();          if (!await context.Users.AnyAsync())         {             var adminUser = new User             {                 Id = Guid.NewGuid(),                 Email = "admin@jtltaskmaster.com",                 PasswordHash = BCrypt.Net.BCrypt.HashPassword("Admin123!"),                 FirstName = "Admin",                 LastName = "User",                 IsActive = true,                 TenantId = Guid.NewGuid(),                 Roles = new List<string> { "Admin" },                 Created = DateTime.UtcNow             };              await context.Users.AddAsync(adminUser);             await context.SaveChangesAsync();         }     } }
